import * as firebase from 'firebase/app';
import 'firebase/app';
export declare type Timestamp = firebase.firestore.Timestamp;
export declare class TimestampDate {
    private tsHandler;
    private strHandler;
    constructor();
    getServerTimestamp(): Timestamp;
    getServerDate(): Date;
    timestampToDate(timestamp: Timestamp): Date;
    dateToTimestamp(date: Date): Timestamp;
    parseTimestampToDate<T, R>(data: T): R;
    parseDateToTimestamp<T, R>(data: T): R;
    parseStringToDate<T, R>(data: T): R;
    parseDateToString<T, R>(data: T): R;
}
